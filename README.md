# AK Auto Show - نظام إدارة معرض السيارات

نظام شامل لإدارة معرض السيارات AK Auto Show، يتضمن نظام تسجيل المشاركين، لوحة إدارة متقدمة، وإدارة الفعاليات والرعاة.

## 🚀 الميزات

### للمشاركين:
- تسجيل سهل ومريح
- رفع صور السيارات
- متابعة حالة التسجيل
- دعم اللغة العربية

### للمدراء:
- لوحة إدارة شاملة
- إدارة المشاركين والموافقة
- إدارة الفعاليات والرعاة
- تصدير البيانات إلى Excel
- إدارة معرض الصور

## 🛠️ التقنيات المستخدمة

- **Frontend:** Next.js 16, React, TypeScript, Tailwind CSS
- **Backend:** Next.js API Routes, Server Actions
- **Database:** Supabase (PostgreSQL)
- **Storage:** Supabase Storage
- **Authentication:** Supabase Auth
- **Email:** Resend API

## 📋 متطلبات النظام

- Node.js 18+
- npm أو yarn
- حساب Supabase
- حساب Resend (للإيميلات)

## 🚀 التثبيت والتشغيل

1. **استنساخ المشروع:**
```bash
git clone https://github.com/mohammedbsmcbh/akautoshow.git
cd akautoshow
```

2. **تثبيت التبعيات:**
```bash
npm install
```

3. **إعداد متغيرات البيئة:**
```bash
cp .env.example .env.local
```

املأ المتغيرات التالية في `.env.local`:
```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
RESEND_API_KEY=your_resend_api_key
```

4. **تشغيل قاعدة البيانات:**
```bash
# تشغيل ملفات SQL في Supabase Dashboard
# 1. complete_database_setup.sql
# 2. create_storage_bucket.sql
# 3. create_storage_policies.sql
```

5. **تشغيل المشروع:**
```bash
npm run dev
```

## 📁 هيكل المشروع

```
├── src/
│   ├── app/
│   │   ├── [locale]/
│   │   │   ├── admin/          # لوحة الإدارة
│   │   │   ├── register/       # صفحة التسجيل
│   │   │   └── terms/          # الشروط والأحكام
│   │   ├── api/                # API Routes
│   │   └── actions.ts          # Server Actions
│   ├── components/             # المكونات المشتركة
│   ├── lib/                    # المكتبات والمساعدات
│   └── middleware.ts           # Middleware
├── public/                     # الملفات الثابتة
└── *.sql                       # ملفات قاعدة البيانات
```

## 🔧 الأوامر المتاحة

```bash
# التطوير
npm run dev

# البناء
npm run build

# التشغيل في الإنتاج
npm start

# فحص الكود
npm run lint
```

## 📊 قاعدة البيانات

### الجداول الرئيسية:
- `events` - الفعاليات
- `registrations` - التسجيلات
- `car_images` - صور السيارات
- `sponsors` - الرعاة
- `gallery_images` - صور المعرض
- `admin_users` - المدراء

### Storage Buckets:
- `car-images` - صور السيارات
- `sponsors-logos` - شعارات الرعاة
- `gallery-images` - صور المعرض

## 🔐 الأمان

- Row Level Security (RLS) مفعل
- Authentication مطلوب للوحة الإدارة
- تحقق من صحة البيانات
- حماية من SQL Injection

## 🌐 الدعم

للدعم الفني أو الأسئلة، يرجى التواصل مع فريق التطوير.

## 📝 الترخيص

هذا المشروع محمي بحقوق الطبع والنشر © 2025 AK Auto Show
