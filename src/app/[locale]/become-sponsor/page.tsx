import BecomeSponsorClient from '@/components/BecomeSponsorClient';

export const metadata = {
  title: 'Become a Sponsor | كن راعياً',
  description: 'Join the premier automotive event in Bahrain as a sponsor. انضم كراعي لأكبر حدث للسيارات في البحرين.',
};

export default function BecomeSponsorPage() {
  return <BecomeSponsorClient />;
}
